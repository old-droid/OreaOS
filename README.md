# OreaOS
An lightweight hacker-focused WebOS

## How to Run

To run OreaOS, you can use a simple Python HTTP server. Navigate to the project's root directory in your terminal and execute the following command:

```bash
python3 -m http.server 5000
```

After running the command, open your web browser and go to `http://localhost:5000` to access the application.
